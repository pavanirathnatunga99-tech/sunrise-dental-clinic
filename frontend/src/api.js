const BASE=import.meta.env.VITE_API_URL||'http://localhost:8080/api';
export async function api(path,options={}){const token=localStorage.getItem('token');const response=await fetch(BASE+path,{...options,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{ }),...options.headers}});if(response.status===401){localStorage.removeItem('token');localStorage.removeItem('user');if(!path.includes('/auth/login'))location.href='/login';}const text=await response.text();const data=text?JSON.parse(text):null;if(!response.ok)throw Object.assign(new Error(data?.message||'Something went wrong. Please try again.'),{fields:data?.fieldErrors||{},status:response.status});return data;}
export const money=v=>new Intl.NumberFormat('en-LK',{style:'currency',currency:'LKR'}).format(v||0);
